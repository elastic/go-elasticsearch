// generated by github.com/elastic/elasticsearch-go/cmd/generator; DO NOT EDIT

package {{.PackageName}}

import (

  {{range .SubPackages}}"{{.PackageRepo}}"
  {{end}}"{{.TransportPackage.PackageRepo}}"

)

// {{.TypeName}} is the {{.PackageName}} API.
type {{.TypeName}} struct {
{{range .SubPackages}}// {{.TypeName}} is the {{.PackageName}} client
  {{.TypeName}} *{{.PackageName}}.{{.TypeName}}
  {{end}}// client is the transport client
  {{.TransportPackage.FieldName}} *{{.TransportPackage.PackageName}}.{{.TransportPackage.TypeName}}
}

// New is the constructor for {{.TypeName}}
func New({{.TransportPackage.ReceiverName}} *{{.TransportPackage.PackageName}}.{{.TransportPackage.TypeName}}) *{{.TypeName}} {
  return &{{.TypeName}}{
    {{range .SubPackages}}{{.TypeName}}: {{.PackageName}}.New({{.TransportPackage.ReceiverName}}),
    {{end}}{{.TransportPackage.FieldName}}: {{.TransportPackage.ReceiverName}},
  }
}

// Option is a {{.TypeName}} option
type Option struct{}
