// generated by github.com/elastic/goelasticsearch/cmd/generator; DO NOT EDIT

package {{.PackageName}}

import (
	"fmt"
	"net/http"

	"github.com/elastic/goelasticsearch/client/transport"
)

// {{.MethodName}}{{.Spec.Documentation}}{{range .RequiredParams}}
//
// {{.Name}}: {{.Description}}{{end}}{{if .Spec.Body}}
//
// body: {{.Spec.Body.Description}}{{end}}{{if .OptionalParams}}
//
// options: optional parameters. Supports the following functional options: {{range .OptionalParams}}{{.OptionName}}, {{end}}see the Option type in this package for more info.{{end}}
func ({{.ReceiverName}} *{{.TypeName}}) {{.MethodName}}({{range .RequiredParams}}{{.Name}} {{.Type}}, {{end}}{{if .Spec.Body}}body map[string]interface{}, {{end}}options ...*Option) (*http.Response, error) {
	supportedOptions := map[string]struct{}{
		{{range .OptionalParams}}"{{.OptionName}}": struct{}{},
	{{end}}}
	req := &http.Request{
		URL: &url.URL{
			Scheme: {{.ReceiverName}}.transport.Scheme,
			Host:   {{.ReceiverName}}.transport.Host,
		},
		Method: "{{.HTTPMethod}}",
	}
	for _, option := range options{
		if _, ok := supportedOptions[option.name]; !ok {
			return nil, fmt.Errorf("unsupported option: %s", option.name)
		}
		option.apply(req)
	}
	return {{.ReceiverName}}.transport.Do(req)
}
