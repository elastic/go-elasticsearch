// generated by github.com/elastic/elasticsearch-go/cmd/generator; DO NOT EDIT

package api

import (

  {{range .Apis}}"{{.PackageRepo}}"
  {{end}}"github.com/elastic/elasticsearch-go/client/http"

)

// API is the root API, that can be embedded in the client to give direct access to all the APIs.
type API struct {
{{range .Apis}}// {{.APIName}} is the {{.PackageName}} client
  {{.APIName}} *{{.PackageName}}.{{.TypeName}}
  {{end}}// client is the transport client
  client *http.Client
}

// New is the constructor for API
func New(c *http.Client) *API {
  return &API{
    {{range .Apis}}{{.APIName}}: {{.PackageName}}.New(c),
    {{end}}client: c,
  }
}
