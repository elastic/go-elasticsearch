// generated by github.com/elastic/go-elasticsearch/cmd/generator; DO NOT EDIT

package {{.Name}}

import (
	"net/http"
)

{{range .Enums}}// {{.Type}} - {{.Description}}
type {{.Type}} int
const (
	{{range .EnumValues}}// {{.Name}} can be used to set {{.Type}} to "{{.SpecName}}"
	{{.Name}} = iota
{{end}})

{{end}}// Option is a non-required API option that gets applied to an HTTP request.
type Option struct {
	name string
	apply func(r *http.Request)
}

{{range .Options}}// {{.OptionName}} - {{.Description}}
func {{.OptionName}}({{.Name}} {{.Type}}) *Option {
  return &Option{
		name: "{{.OptionName}}",
		apply: func(r *http.Request) {
  	},
	}
}

{{end}}var (
supportedOptions = map[string]map[string]struct{}{
{{range .Methods}}"{{.MethodName}}": map[string]struct{}{
{{range .OptionalURLParts}}"{{.OptionName}}": struct{}{},
	{{end}}{{range .OptionalURLParams}}"{{.OptionName}}": struct{}{},
	{{end}}
},
{{end}}
}
)
